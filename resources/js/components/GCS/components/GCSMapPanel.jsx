import React from 'react';

/**
 * GCSMapPanel — DEPRECATED / STUB
 * Camera Feed & Radar sudah dipindah ke GCSLeftPanel.
 * Komponen ini dipertahankan sebagai stub agar tidak ada broken import.
 * Tidak dirender (topMapPanel={null} di AppGCS).
 */
const GCSMapPanel = () => null;

export default GCSMapPanel;
